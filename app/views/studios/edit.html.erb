<%# <%= render "form", task: @studio %>
<div class="site">
  <div class="container-sm">
    <div class="d-flex flex-column m-5">
      <h1>EDIT YOUR STUDIO 🎥</h1>
      <hr>
      <%= simple_form_for @studio do |f| %>
        <div class="form-inputs">
          <%= f.input :name %>
          <%= f.input :address %>
          <%= f.input :details, prepend: "Some studio details" %>
          <div class="m-3"><%= f.input :photo, as: :file %></div>
        </div>
        <div class="form-actions">
          <%= link_to "Cancel", :back, class:"btn btn-outline-warning" %>
          <%= f.button :submit, class:"btn btn-outline-dark" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
